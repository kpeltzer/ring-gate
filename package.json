{
	"name": "ring-gate",
	"version": "1.0.0",
	"engines": {
		"node": ">=21.0.0"
	},
	"description": "A simple script to monitor if a Ring contact sensor is left open.",
	"main": "script.js",
	"scripts": {
		"build": "tsc",
		"dev": "node --env-file=.env --watch -r ts-node/register src/script.ts",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "Ken Peltzer",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.12.11",
		"ts-node": "^10.9.2",
		"typescript": "^5.4.5"
	},
	"dependencies": {
		"ring-client-api": "^12.1.0"
	}
}
